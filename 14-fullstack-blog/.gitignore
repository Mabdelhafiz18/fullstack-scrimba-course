# Dependencies (installed separately in server/ and client/)
**/node_modules

# Frontend build output
client/dist
*.local
.vite

*.log

# Environment files (DATABASE_URL etc.) — never commit real secrets.
.env
